<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Let the PHP API handle its own routes.
  RewriteRule ^api(/.*)?$ - [L]

  # Serve existing files and directories directly.
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # React SPA fallback.
  RewriteRule ^ index.html [L]
</IfModule>
